export class Cell {
    constructor(value, rowPos, columnPos, selected) {
        this.value = value;
        this.rowPos = rowPos;
        this.columnPos = columnPos;
        this.selected = selected;
    }
}
